interface:
  display_name: "Portfolio"
  short_description: "Repo-specific patterns and workflows for Portfolio"
  default_prompt: "Use the Portfolio repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true